---
import defaultImage from '@/assets/images/david-d-lawson-profile.png';
import { SITE } from '@/config';
import type { MetaSEO } from '@/types';
import { getRelativeUrlByFilePath } from '@/utils/directories';
import { AstroSeo } from '@astrolib/seo';
import { Font } from 'astro:assets';
export interface Props extends MetaSEO {
  dontUseTitleTemplate?: boolean;
}

const {
  title = SITE.name,
  description = SITE.description,
  image: _image = defaultImage,
  canonical = Astro.url.pathname,
  noindex = false,
  nofollow = false,
  ogTitle = title,
  ogType = 'website',
  dontUseTitleTemplate = false,
} = Astro.props;

const image =
  typeof _image === 'string'
    ? new URL(_image, Astro.site)
    : _image && typeof _image['src'] !== 'undefined'
      ? new URL(getRelativeUrlByFilePath(_image.src), Astro.site)
      : null;
---

<meta charset="UTF-8" />
<meta
  name="viewport"
  content="width=device-width"
/>

<Font
  cssVariable="--font-switzer"
  preload
/>

<AstroSeo
  title={title}
  titleTemplate={dontUseTitleTemplate ? '%s' : `%s — ${SITE.name}`}
  description={description}
  canonical={String(canonical)}
  noindex={noindex}
  nofollow={nofollow}
  openGraph={{
    url: String(canonical),
    title: ogTitle,
    description: description,
    type: ogType,
    images: image
      ? [
          {
            url: image.toString(),
            alt: ogTitle,
          },
        ]
      : undefined,
    site_name: 'David D Lawson',
  }}
  twitter={{
    handle: '@ddlawson',
    // site: '@site',
    cardType: image ? 'summary_large_image' : undefined,
  }}
/>

<link
  rel="icon"
  type="image/svg+xml"
  href="/favicon.ico"
/>
<link
  rel="apple-touch-icon"
  sizes="180x180"
  href="/apple-touch-icon.png"
/>
<link
  rel="icon"
  type="image/png"
  sizes="32x32"
  href="/favicon-32x32.png"
/>
<link
  rel="icon"
  type="image/png"
  sizes="16x16"
  href="/favicon-16x16.png"
/>
<link
  rel="manifest"
  href="/site.webmanifest"
/>
<link
  rel="sitemap"
  href="/sitemap-index.xml"
/>
<link
  rel="alternate"
  type="application/rss+xml"
  title=`${SITE.name} RSS Feed`
  href={new URL('rss.xml', Astro.site)}
/>

<!-- TODO: NEED TO ADD PROPER FAVICONS HERE -->

<!-- Light/dark mode   -->
<script is:inline>
  // Apply theme immediately to prevent FOUC
  (function () {
    const theme = localStorage.getItem('theme');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    if (theme === 'dark' || (!theme && prefersDark)) {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }
  })();

  // Critical: preserve theme across view transitions by applying to the new document before swap
  document.addEventListener('astro:before-swap', (event) => {
    // Transfer the current theme to the incoming document
    if (document.documentElement.classList.contains('dark')) {
      event.newDocument.documentElement.classList.add('dark');
    } else {
      event.newDocument.documentElement.classList.remove('dark');
    }
  });

  function updateThemeIcon() {
    const isDark = document.documentElement.classList.contains('dark');
    const themeIcon = document.getElementById('theme-icon');
    if (themeIcon) {
      themeIcon.setAttribute('name', isDark ? 'tabler:sun' : 'tabler:moon');
    }
  }

  function attachThemeToggleHandler() {
    const toggle = document.querySelector('[data-aw-toggle-color-scheme]');
    if (toggle) {
      // Remove potential old listener before adding new one
      toggle.onclick = null;
      toggle.onclick = function () {
        const isDark = document.documentElement.classList.toggle('dark');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
        updateThemeIcon(); // Update icon immediately on click
      };
    }
  }

  function initializeThemeUI() {
    updateThemeIcon(); // Set initial icon state
    attachThemeToggleHandler(); // Attach click handler
  }

  // Initialize on first load
  document.addEventListener('DOMContentLoaded', initializeThemeUI);
  // Re-initialize after view transitions
  document.addEventListener('astro:after-swap', initializeThemeUI);
</script>

<!-- Umami Analytics -->
{
  import.meta.env.PROD && (
    <script
      async
      defer
      is:inline
      data-website-id={SITE.umamiId}
      src="https://cloud.umami.is/script.js"
    />
  )
}

<!-- FeedbackFish -->
<script
  defer
  is:inline
  src="https://feedback.fish/ff.js?pid=e8f83126219490"
></script>
