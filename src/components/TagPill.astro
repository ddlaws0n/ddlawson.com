---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<span
  class="rounded-sm border border-border px-2.5 py-0.5 font-display text-[0.52rem] font-bold tracking-[0.15em] whitespace-nowrap text-text-muted uppercase"
>
  {tag}
</span>
