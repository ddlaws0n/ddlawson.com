<div class="m-divider"></div>
<section class="m-quote m-reveal">
	<span class="m-quote-mark">&ldquo;</span>
	<p class="m-quote-text">
		The gap between a great product and a successful customer is someone who <em>gives a damn</em>.
	</p>
	<div class="m-quote-rule"></div>
	<span class="m-quote-attr">David D. Lawson</span>
</section>
<div class="m-divider"></div>

<style>
	.m-quote {
		padding: 5.5rem 0;
		text-align: center;
		position: relative;
	}

	.m-quote-mark {
		font-family: var(--m-display);
		font-size: 8rem;
		font-weight: 800;
		line-height: 0;
		color: var(--m-blue);
		opacity: 0.06;
		position: absolute;
		top: 3.5rem;
		left: 50%;
		transform: translateX(-50%);
		pointer-events: none;
		user-select: none;
	}

	.m-quote-text {
		font-family: var(--m-display);
		font-size: clamp(1.35rem, 2.6vw, 2.1rem);
		font-weight: 500;
		line-height: 1.5;
		color: var(--m-text);
		max-width: 32ch;
		margin: 0 auto 1.5rem;
		position: relative;
	}

	.m-quote-text em {
		font-style: normal;
		color: var(--m-blue-bright);
	}

	.m-quote-rule {
		width: 32px;
		height: 1px;
		background: var(--m-blue-muted);
		margin: 0 auto 1.25rem;
	}

	.m-quote-attr {
		font-family: var(--m-body);
		font-size: 0.65rem;
		font-weight: 400;
		letter-spacing: 0.2em;
		text-transform: uppercase;
		color: var(--m-text-muted);
	}

	@media (max-width: 768px) {
		.m-quote {
			padding: 4rem 0;
		}

		.m-quote-text {
			font-size: clamp(1.15rem, 4.5vw, 1.5rem);
		}

		.m-quote-mark {
			font-size: 5rem;
		}
	}
</style>
