---
import { NAV } from '@/site.config';

const isActive = (href: string) => {
  const pathname = new URL(Astro.request.url).pathname;
  return pathname === href || (href !== '/' && pathname.startsWith(href));
};
---

<header
  class="z-nav sticky top-0 flex items-center justify-between border-b border-border bg-bg/85 px-12 py-[1.1rem] backdrop-blur-nav max-lg:px-6"
>
  <a
    href="/"
    class="font-display text-subheading font-extrabold tracking-tight text-text no-underline"
    aria-label="Homepage"
  >
    DDL
  </a>

  <ul class="m-0 hidden list-none gap-10 p-0 lg:flex" id="main-nav-links">
    {
      NAV.map(({ name, href }) => (
        <li>
          <a
            href={href}
            class:list={[
              'font-body text-caption tracking-label uppercase no-underline transition-colors duration-200',
              isActive(href) ? 'text-text' : 'text-text-muted hover:text-text',
            ]}
          >
            {name}
          </a>
        </li>
      ))
    }
  </ul>

  <span class="hidden font-body text-caption font-light tracking-tight text-text-muted opacity-50 lg:block">
    London, 2026
  </span>

  <button
    id="mobile-menu-toggle"
    type="button"
    class="flex min-h-[44px] min-w-[44px] flex-col items-center justify-center gap-1.5 text-text-muted lg:hidden"
    aria-label="Toggle menu"
    aria-expanded="false"
    aria-controls="mobile-menu"
  >
    <span class="block h-0.5 w-5 bg-current transition-all" id="ham-top"></span>
    <span class="block h-0.5 w-5 bg-current transition-all" id="ham-mid"></span>
    <span class="block h-0.5 w-5 bg-current transition-all" id="ham-bot"></span>
  </button>
</header>

<div
  id="mobile-menu"
  hidden
  class="z-nav fixed inset-x-0 top-[57px] border-b border-border bg-bg/95 px-6 py-4 backdrop-blur-nav lg:hidden"
>
  <ul class="m-0 flex list-none flex-col gap-4 p-0">
    {
      NAV.map(({ name, href }) => (
        <li>
          <a
            href={href}
            class:list={[
              'block min-h-[44px] py-2 font-body text-caption tracking-label uppercase no-underline transition-colors duration-200',
              isActive(href) ? 'text-text' : 'text-text-muted hover:text-text',
            ]}
          >
            {name}
          </a>
        </li>
      ))
    }
  </ul>
</div>

<script>
  const toggle = document.getElementById('mobile-menu-toggle') as HTMLButtonElement | null;
  const menu = document.getElementById('mobile-menu') as HTMLElement | null;

  toggle?.addEventListener('click', () => {
    const isOpen = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!isOpen));
    if (menu) menu.hidden = isOpen;
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && toggle?.getAttribute('aria-expanded') === 'true') {
      toggle.setAttribute('aria-expanded', 'false');
      if (menu) menu.hidden = true;
      toggle.focus();
    }
  });
</script>
